# Лабораторна робота №11

## ТЕМА:Обробка сигналів в операційній системі Linux. Використання альтернативного стеку сигналів.
# Варіант 3
Напишіть обробник SIGBUS, який розрізняє помилки через mmap та помилки доступу до фізичної пам’яті..
##  Мета роботи

Розробити програму на мові C, яка обробляє сигнал **SIGBUS** та дозволяє розрізняти причини його виникнення:
- помилки, пов’язані з `mmap()`
- помилки доступу до фізичної пам’яті
## Теоретичні відомості

**SIGBUS** — це сигнал, який генерується операційною системою при спробі доступу до пам’яті, яка не може бути коректно оброблена процесором або ОС.

Основні причини SIGBUS:

- доступ до неіснуючої фізичної пам’яті
- помилки роботи з `mmap()` (наприклад, доступ за межі відображеного файлу)
- неправильне вирівнювання пам’яті

### Різниця між SIGBUS і SIGSEGV:

- **SIGSEGV** — доступ до невалідної віртуальної пам’яті
- **SIGBUS** — доступ до проблемної фізичної або mmap-області

## Реалізація обробника SIGBUS

### Код програми (ex1.c)

```c
#include <stdio.h>
#include <stdlib.h>
#include <signal.h>
#include <string.h>
#include <unistd.h>
#include <sys/mman.h>
#include <fcntl.h>
#include <sys/stat.h>

void handler(int sig, siginfo_t *info, void *context)
{
    (void)sig;
    (void)context;

    printf("\n=== SIGBUS detected ===\n");

    if (info->si_code == BUS_ADRERR)
    {
        printf("Причина: помилка доступу до фізичної пам'яті або вирівнювання\n");
    }
    else if (info->si_code == BUS_OBJERR)
    {
        printf("Причина: помилка mmap або відображеного файлу\n");
    }
    else
    {
        printf("Невідома причина SIGBUS\n");
    }

    printf("Адреса помилки: %p\n", info->si_addr);
    exit(1);
}

int main()
{
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));

    sa.sa_sigaction = handler;
    sa.sa_flags = SA_SIGINFO;

    sigaction(SIGBUS, &sa, NULL);

    printf("Створення mmap області...\n");

    int fd = open("testfile.bin", O_RDWR | O_CREAT, 0666);
    ftruncate(fd, 100);

    char *map = mmap(NULL, 4096, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);

    printf("Спроба доступу за межі mmap...\n");

    map[5000] = 'A';

    return 0;
}
```
### Компіляція 
```
gcc ex1.c -o ex1 -Wall -Wextra
```
##  Пояснення роботи

У програмі реалізовано:

- обробник сигналу SIGBUS через `sigaction`
- аналіз причини помилки через `si_code`
- створення memory-mapped області через `mmap`
- навмисний вихід за межі реального розміру файлу
- генерація SIGBUS та його перехоплення

##  Створення тестового файлу
```
arthas@ubuntuserver:~/projects/pr11$ echo "hello" > testfile.bin
```

## Запуск програми

arthas@ubuntuserver:~/projects/pr11$ ./ex1

---

## Результат

Створення mmap області...  
Спроба доступу за межі mmap...

=== SIGBUS detected ===  
Причина: помилка mmap або відображеного файлу  
Адреса помилки: 0x...

---

## 9. Аналіз результату

Програма демонструє виникнення SIGBUS при некоректному доступі до memory-mapped області.

Файл має розмір 100 байт, але mmap створено на 4096 байт, тому доступ за межі реального файлу викликає SIGBUS.

Сигнал перехоплюється обробником, який визначає тип помилки через `si_code`.

---

## 10. Висновок

У ході роботи було реалізовано обробник сигналу SIGBUS у Linux. Було досліджено механізм виникнення помилок при роботі з `mmap`, а також показано різницю між SIGBUS і SIGSEGV. Програма демонструє принцип обробки помилок доступу до пам’яті та дозволяє аналізувати їх причини.
